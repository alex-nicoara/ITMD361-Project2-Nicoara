$.noConflict();

// Reintroduce the dolalr sign scope to self executing function
(function($) {
  // variables
  var player;
  var computer;
  var hasWon;
  var board;
  var spaces_available;

  // actions

  // functions
})(jQuery);
